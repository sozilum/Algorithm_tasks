use std::collection::HashMap;

fn main(){
	pub fn remove_dublicates(&mut nums: Vec<i32>) -> i32{
		let mut index_vector: Vec<i32> - vec![];
		let mut aux: i32 = &nums[0];
		// задача, изменить вектор, убрать дуплиаты и вернуть значение после
		// после которого идёт правильный вектор
		
		for (index, num) in &mut nums.iter().enumerate(){

		if aux == num{ }
		}
	}
}
